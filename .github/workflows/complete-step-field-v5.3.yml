name: Complete compact Step Field v5.3

on:
  push:
    branches:
      - feature/step-field-v5.3
    paths:
      - apply-step-field-v5.3.py
      - .github/workflows/complete-step-field-v5.3.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: complete-step-field-v5-3-${{ github.ref }}
  cancel-in-progress: false

jobs:
  complete-and-test:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Apply Step Field integration
        run: python3 apply-step-field-v5.3.py

      - name: Run complete repository checks
        run: npm run check

      - name: Remove one-use completion files
        run: |
          rm -f apply-step-field-v5.3.py
          rm -f .github/workflows/complete-step-field-v5.3.yml

      - name: Commit verified Step Field
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "Add compact Step Field projection"
          git push origin "HEAD:${GITHUB_REF_NAME}"
